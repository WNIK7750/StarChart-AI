"""Agent module boundary.

This package is intentionally split into small layers so future Agent features
can be rewritten independently: routing, tools, memory, evaluation and service
orchestration should not share mutable business state.
"""
