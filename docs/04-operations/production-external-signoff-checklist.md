# 生产外部签收清单

> 状态：外部阻塞 / 未验证  
> 日期：2026-07-25  
> 规则：本清单只由拥有真实环境、费用授权和职责授权的人员执行。不得把本地或模拟结果
> 填成通过；证据不得包含密钥、用户内容、Authorization、Cookie 或 Provider 正文。

## 签收表

| 项目 | 责任人 | 所需输入/授权 | 受控执行命令或动作 | 成功条件 | 失败与回滚条件 | 证据位置 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Provider 条款与数据处理 | 合规负责人 | 当前百炼条款、DPA、训练/留存/删除/地域说明 | 在合规工单中逐条核对 `agent-provider-stage1-release-checklist.md` A 节；不调用 API | 中国内地处理、训练使用、留存、删除、支持访问与隐私告知均有书面结论 | 任一项不清楚：Live 保持 `0`，Provider 保持 `deterministic` | `________` | [ ] 未验证 |
| 北京空间与出口白名单 | 云账户管理员 + 网络运维 | 默认业务空间、专用可撤销 Key、上海实际固定出口 IP | 由运维从目标主机执行批准的出口探测；在控制台只允许该 IP 和 `qwen3.5-flash` | Provider 主机为批准的 `*.cn-beijing.maas.aliyuncs.com`；非白名单出口被拒绝 | 域名、地域或出口漂移：撤销 Key、Live=`0` | `________` | [ ] 未验证 |
| 费用硬限制与告警 | 云账户管理员 | 100 CNY 以内硬上限授权、告警接收人 | 控制台关闭自动充值并配置 50/80/100 CNY 告警；保存脱敏审计记录 | 达到硬上限后请求失败；负责人能收到三级告警 | 无硬上限或可欠费：撤销 Key，不允许真实调用 | `________` | [ ] 未验证 |
| 真实邮件/短信恢复通道 | 产品/合规 + 身份运维 | 已验证域名/号码、DPA、退信/失败与安全通知方案 | 在隔离账号上执行开始、送达、单次确认、过期、重放和停用发送端演练 | 公开响应仍不可枚举；只发送到已验证通道；凭据单次/过期有效；日志无目标与凭据 | 投递或隐私任一不符：恢复发送端禁用，账号恢复保持 fail-closed | `________` | [ ] 未验证 |
| 受控真实 Provider 调用 | 发布负责人 + 评测负责人 | 非生产专用 Key、无个人信息样例、费用窗口、回滚负责人 | 在隔离配置中临时设 Provider=`openai_compatible`、Live=`1`、模型=`qwen3.5-flash`、升级比例=`0`，通过应用测试客户端执行一次；结束立即 Live=`0` | 只记录 request ID、分类、模型、Token、费用、延迟；输出守卫通过；普通页面不受影响 | 超时、越界、敏感数据、错误模型或成本异常：Live=`0`、Provider=`deterministic`、撤销 Key | `________` | [ ] 未验证 |
| 目标 C4G 容量与上海到北京链路 | 性能负责人 + 运维 | 与生产同规格隔离环境、批准负载模型、监控与停止阈值 | `<approved-load-tool> --target <staging-base-url> --profile <approved-profile> --duration <window>`；同时采集 CPU/内存/队列/5xx/延迟 | 预先批准的吞吐与 p95/p99、CPU、内存、队列、错误率全部达标，且不使用真实用户数据 | 任一停止阈值触发：立即终止负载，Live=`0`；不得外推本地基准 | `________` | [ ] 未验证 |
| 生产发布前备份 | 数据库负责人 | 冻结写入窗口、生产数据库路径、加密备份位置 | `python scripts/manage-users-backup.py backup --source <production-db> --output <encrypted-backup-path>`；随后执行 `verify` | integrity `ok`、外键 0、20 个迁移与 checksum 一致；备份访问受控 | 校验失败：停止发布，不替换数据库 | `________` | [ ] 未验证 |
| 生产恢复演练 | 数据库负责人 + 发布负责人 | 隔离恢复目标、已验证备份、恢复窗口 | `python scripts/manage-users-backup.py restore --backup <verified-backup> --target <isolated-restore-db>`；运行 health、迁移与 canary | 恢复库完整、外键 0、checksum 一致，关键 API smoke 通过 | 任一失败：保留故障证据，禁止对生产目标执行 `--replace` | `________` | [ ] 未验证 |
| 应用与 Provider 回滚 | 发布负责人 | 上一应用归档、变更单、开关权限、回滚负责人 | Live=`0` → Provider=`deterministic` → 关闭 SSE/Sessions（如需）→ 回滚应用版本 → live/ready 与公共 Learning/Tools smoke | 回滚时限内普通页面、公开内容、登录和确定性 Agent 恢复；不执行破坏性 down migration | 普通功能未恢复：冻结写入，按已验证 pre-restore 备份恢复 | `________` | [ ] 未验证 |
| 告警与值守 | 运维负责人 | 值守表、告警渠道、升级路径 | 在隔离窗口触发合成 5xx、Provider 回退、预算拒绝与 ready 失败；不使用真实内容 | 接收、确认、升级、关闭均在批准时限内，日志无敏感正文 | 告警漏报或正文泄露：NO-GO，修复后重演 | `________` | [ ] 未验证 |

## 最终决定

- [ ] **GO**：以上所有项目由相应责任人完成，证据可复核，回滚负责人在线。
- [x] **NO-GO（当前默认）**：缺少真实环境、云/费用授权、合规签收和生产负责人，
  未执行任何付费或生产验证。

最终批准人：`________`　日期：`________`　变更单：`________`  
回滚负责人：`________`　值守窗口：`________`
